# Ignore everything in the .codex directory by default
*

# Allow the .gitignore itself
!.gitignore

# Allow the root AGENTS.md (Orchestrator)
!AGENTS.md

# Allow the roles directory and all SKILL.md files within it
!roles/
!roles/**/
!roles/**/SKILL.md

# Explicitly ignore the rules folder as requested
/rules/
